export default function Loading() {
  return (
    <main className="mx-auto w-full max-w-6xl px-5 py-10 sm:px-8">
      <section className="panel">
        <p className="section-title">Loading learning context...</p>
        <p className="section-copy">Preparing runtime models and architecture walkthroughs.</p>
      </section>
    </main>
  );
}
